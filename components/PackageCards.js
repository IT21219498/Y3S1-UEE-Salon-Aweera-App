import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const PackageCards = () => {
  return (
    <View>
      <Text>PackageCards</Text>
    </View>
  );
};

export default PackageCards;

const styles = StyleSheet.create({});
